mutation contactCreate($input: ContactCreateMutationInput!) {
    contactCreate(input: $input) {
        contact {
            emailOrPhone
        }
        errors {
            field
            messages
        }
    }
}
