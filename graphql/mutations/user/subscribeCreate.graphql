mutation subscribeCreate($input: SubscribeCreateMutationInput!) {
    subscribeCreate(input: $input) {
        subscribe {
            email
        }
        errors {
            field
            messages
        }
    }
}
