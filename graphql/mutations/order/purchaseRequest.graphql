mutation purchaseRequest(
    $purchaseRequest: PurchaseRequestInput
) {
    purchaseRequest(
        input: { purchaseRequest: $purchaseRequest }
    ) {
        errors {
            field
            messages
        }
    }
}
